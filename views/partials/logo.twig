{#
  Logo du site. Remplace le SVG ci-dessous par ton propre logo.
  - Garde `fill="currentColor"` (ou stroke) pour qu'il hérite de la couleur du texte.
  - Ajuste la hauteur via la classe `h-8` (largeur auto).
#}
<a href="{{ site.url }}" class="inline-flex items-center" aria-label="{{ site.name }} — Accueil">
  <img src="{{ theme.link ~ '/assets/onzehonze-logo-2.svg' }}" alt="{{ site.name }}" class="h-32 w-auto">
</a>
